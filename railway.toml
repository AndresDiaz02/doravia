[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "pnpm --filter @workspace/api run start"
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
